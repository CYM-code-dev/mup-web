# mup-web 运行时依赖（pandas/streamlit 仅 parity 回归测试用，不在此列；
# 如需跑 parity，额外: pip install pandas streamlit）
fastapi
uvicorn[standard]
python-multipart   # FastAPI 文件上传 (UploadFile/File) 必需，FastAPI 不自带
python-docx
openpyxl
Pillow
requests
ddddocr        # LIMS 验证码 OCR（lims_auto_login.py 懒加载；体积大，含 opencv/onnxruntime）
truststore     # 让 urllib 信任 Windows 证书库 (公司 SSL 拦截自签 CA); 缺则回退 certifi
